name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Ping mỗi 5 phút

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s https://chillnet-backend.onrender.com/health
